<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xsi:schemaLocation="
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.1.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">

    <bean class="org.springframework.mail.javamail.JavaMailSenderImpl" id="mailSender">
        <property name="host" value="${email.host:mailout.telia.com}"/>
        <property name="protocol" value="${email.protocol:smtps}"/>
        <property name="port" value="${email.port:465}"/>
        <property name="username" value="${email.username:u09003390}"/>
        <property name="password" value="${email.password:Z2JSAQKf}"/>
    </bean>
    <bean class="org.springframework.mail.SimpleMailMessage" id="templateMessage">
        <property name="from" value="${email.from:tps@arnellconsulting.com}"/>
        <property name="subject" value="${email.subject:Registration confirmation}"/>
    </bean>
</beans>